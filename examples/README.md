# Examples

- `quickstart.jl` — construct a stream, analyze, explain
- `showcase.jl` — twelve-month synthetic reconstruction

```bash
julia --project examples/quickstart.jl
julia --project examples/showcase.jl
```
